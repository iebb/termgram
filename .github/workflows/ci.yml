name: CI

on:
  push:
    branches:
      - main
      - master
  pull_request:

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  version:
    name: Plan commit versions
    runs-on: ubuntu-latest
    outputs:
      plan: ${{ steps.version.outputs.plan }}
      has_releases: ${{ steps.version.outputs.has_releases }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Derive versions from mainline commit heights
        id: version
        shell: bash
        env:
          AFTER_SHA: ${{ github.sha }}
          DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          test "$(git rev-parse --is-shallow-repository)" = "false"

          : > release-plan.tsv
          if [[ "$GITHUB_EVENT_NAME" == push && "$GITHUB_REF_NAME" == "$DEFAULT_BRANCH" ]]; then
            gh api --paginate "repos/$GITHUB_REPOSITORY/releases?per_page=100" \
              --jq '.[] | select(.draft == false) | .tag_name' > published-tags.txt
            height=0
            while IFS= read -r sha; do
              height=$((height + 1))
              # Automatic publishing began at height 2. Older source did not
              # yet contain the build-version entry point.
              if ((height < 2)); then
                continue
              fi
              tag="v0.1.$height"
              if grep -Fxq "$tag" published-tags.txt; then
                test "$(git rev-list -n 1 "$tag")" = "$sha"
              else
                printf '%s\t%s\t0.1.%s\n' "$sha" "$height" "$height" \
                  >> release-plan.tsv
              fi
            done < <(git rev-list --first-parent --reverse "$AFTER_SHA")
          else
            height="$(git rev-list --first-parent --count "$AFTER_SHA")"
            printf '%s\t%s\t0.1.%s\n' "$AFTER_SHA" "$height" "$height" \
              >> release-plan.tsv
          fi
          plan="$(jq -Rsc '
            split("\n")
            | map(select(length > 0) | split("\t")
                | {sha: .[0], height: .[1], version: .[2]})
          ' release-plan.tsv)"
          echo "plan=$plan" >> "$GITHUB_OUTPUT"
          if [[ "$plan" == '[]' ]]; then
            echo "has_releases=false" >> "$GITHUB_OUTPUT"
          else
            echo "has_releases=true" >> "$GITHUB_OUTPUT"
          fi
          cat release-plan.tsv

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-plan
          path: release-plan.tsv
          if-no-files-found: error
          retention-days: 14

  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Read pinned Rust toolchain
        id: toolchain
        shell: bash
        run: |
          channel="$(sed -n 's/^channel = "\(.*\)"/\1/p' rust-toolchain.toml)"
          test -n "$channel"
          printf 'channel=%s\n' "$channel" >> "$GITHUB_OUTPUT"
      - uses: dtolnay/rust-toolchain@02cb101ec7c40f2c49e1d9714d64511d8e1b74de # master
        with:
          toolchain: ${{ steps.toolchain.outputs.channel }}
          components: clippy,rustfmt
      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
      - name: Check installer scripts
        shell: bash
        run: bash -n scripts/install.sh
      - name: Verify package metadata publishing
        run: python3 scripts/test-package-metadata.py
      - name: Validate bilingual Wiki sources
        run: python3 scripts/export-wiki.py --check
      - name: Parse PowerShell installer
        shell: pwsh
        run: |
          $tokens = $null
          $errors = $null
          [void][System.Management.Automation.Language.Parser]::ParseFile(
            (Resolve-Path scripts/install.ps1).Path,
            [ref]$tokens,
            [ref]$errors
          )
          if ($errors.Count -ne 0) {
            $errors | ForEach-Object { Write-Error $_.Message }
            exit 1
          }
      - run: cargo fmt --all -- --check
      - run: cargo clippy --locked --all-targets --all-features -- -D warnings
      - run: cargo test --locked --all-targets --all-features
      - name: Verify own-deletion update payloads
        run: >-
          cargo test --locked --manifest-path vendor/grammers-mtsender/Cargo.toml
          --lib affected_updates_keep_payloads --target-dir target/mtsender-regression
      - name: Verify upstream SQLite close-ownership regression
        run: >-
          cargo test --locked --manifest-path vendor/libsql/Cargo.toml
          --no-default-features --features core --lib
          drop_closes_the_handle_exactly_once --target-dir target/libsql-regression

  build:
    name: Build (${{ matrix.platform }})
    needs:
      - version
      - check
    if: needs.version.outputs.has_releases == 'true'
    strategy:
      fail-fast: false
      matrix:
        include:
          # Keep the three unqualified platform labels for compatibility with
          # auto-updaters shipped before architecture-specific assets existed.
          - platform: linux
            os: ubuntu-latest
            extension: tar.gz
            target: x86_64-unknown-linux-musl
            macos_deployment_target: ""
          - platform: linux-aarch64
            os: ubuntu-24.04-arm
            extension: tar.gz
            target: aarch64-unknown-linux-musl
            macos_deployment_target: ""
          - platform: macos
            os: macos-latest
            extension: tar.gz
            target: aarch64-apple-darwin
            macos_deployment_target: "11.0"
          - platform: macos-x86_64
            os: macos-15-intel
            extension: tar.gz
            target: x86_64-apple-darwin
            macos_deployment_target: "11.0"
          - platform: windows
            os: windows-latest
            extension: zip
            target: x86_64-pc-windows-msvc
            macos_deployment_target: ""
          - platform: windows-aarch64
            os: windows-11-arm
            extension: zip
            target: aarch64-pc-windows-msvc
            macos_deployment_target: ""
    runs-on: ${{ matrix.os }}
    env:
      RELEASE_PLAN: ${{ needs.version.outputs.plan }}
      MACOSX_DEPLOYMENT_TARGET: ${{ matrix.macos_deployment_target }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Read pinned Rust toolchain
        id: toolchain
        shell: bash
        run: |
          channel="$(sed -n 's/^channel = "\(.*\)"/\1/p' rust-toolchain.toml)"
          test -n "$channel"
          printf 'channel=%s\n' "$channel" >> "$GITHUB_OUTPUT"
      - uses: dtolnay/rust-toolchain@02cb101ec7c40f2c49e1d9714d64511d8e1b74de # master
        with:
          toolchain: ${{ steps.toolchain.outputs.channel }}
          targets: ${{ matrix.target }}
      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          shared-key: ${{ matrix.platform }}
      - name: Install static Linux linker
        if: startsWith(matrix.platform, 'linux')
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install --yes musl-tools
      - name: Verify release credentials
        if: github.event_name == 'push' && github.ref_name == github.event.repository.default_branch
        shell: bash
        env:
          TELEGRAM_API_ID: ${{ secrets.TELEGRAM_API_ID }}
          TELEGRAM_API_HASH: ${{ secrets.TELEGRAM_API_HASH }}
        run: |
          set -euo pipefail
          [[ "$TELEGRAM_API_ID" =~ ^[0-9]+$ ]]
          [[ "$TELEGRAM_API_HASH" =~ ^[0-9a-fA-F]{32}$ ]]
      - name: Install from Git and package every pushed commit
        shell: bash
        env:
          VERGEN_GIT_BRANCH: ${{ github.head_ref || github.ref_name }}
          TERMGRAM_EMBEDDED_API_ID: ${{ github.event_name == 'push' && github.ref_name == github.event.repository.default_branch && secrets.TELEGRAM_API_ID || '' }}
          TERMGRAM_EMBEDDED_API_HASH: ${{ github.event_name == 'push' && github.ref_name == github.event.repository.default_branch && secrets.TELEGRAM_API_HASH || '' }}
        run: |
          set -euo pipefail
          mkdir -p dist
          source_url="$(python3 -c 'from pathlib import Path; print(Path.cwd().as_uri())')"
          install_root="$(pwd)/dist/cargo-install"
          while IFS=$'\t' read -r sha version; do
            sha="${sha%$'\r'}"
            version="${version%$'\r'}"
            git checkout --detach "$sha"
            # Historical commits may pin a different toolchain from the PR head.
            rustup target add "${{ matrix.target }}"
            export TERMGRAM_BUILD_VERSION="$version"
            cargo test --locked --all-targets --all-features
            # Exercise Cargo's Git checkout, path patches, install tracking and
            # release profile. Keep immutable Git-source artifacts separate from
            # workspace builds, including build-script executables and metadata.
            cargo install --locked --git "$source_url" --rev "$sha" \
              --bin tg --target "${{ matrix.target }}" --target-dir target/cargo-install \
              --root "$install_root" --force termgram
            binary="$install_root/bin/tg"
            if [[ "$RUNNER_OS" == Windows ]]; then
              binary+=".exe"
            fi
            version_output="$("$binary" --version)"
            version_line="${version_output%%$'\n'*}"
            version_line="${version_line%$'\r'}"
            # The release plan may include historical commits with one-line output.
            case "$version_line" in
              "version  $version" | "tg $version" | "tg $version($(git rev-parse --short HEAD)) $VERGEN_GIT_BRANCH" | "tg $version ($(git rev-parse --short HEAD)) · $VERGEN_GIT_BRANCH") ;;
              *) echo "Unexpected version output: $version_output" >&2; exit 1 ;;
            esac
            if [[ "$RUNNER_OS" == Linux ]]; then
              if readelf -d "$binary" | grep -q '(NEEDED)'; then
                echo "Linux release unexpectedly has dynamic dependencies" >&2
                exit 1
              fi
            fi

            if [[ "$RUNNER_OS" == Windows ]]; then
              (
                cd "$install_root/bin"
                7z a -tzip \
                  "../../termgram-$version-${{ matrix.platform }}.zip" tg.exe
              )
            else
              tar -C "$install_root/bin" -czf \
                "dist/termgram-$version-${{ matrix.platform }}.tar.gz" tg
            fi
          done < <(jq -r '.[] | [.sha, .version] | @tsv' <<<"$RELEASE_PLAN")
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: tg-${{ matrix.platform }}
          path: dist/termgram-*-${{ matrix.platform }}.${{ matrix.extension }}
          if-no-files-found: error
          retention-days: 14
